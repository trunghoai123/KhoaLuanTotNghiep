import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";

const ContactStyles = styled.div``;

const Contact = (props) => {
  return <ContactStyles></ContactStyles>;
};

Contact.propTypes = {};

export default Contact;
